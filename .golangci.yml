version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
  disable:
    - errcheck

  settings:
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-ST1000"
